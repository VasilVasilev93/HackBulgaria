import containdigit.py
def contains_digits(number, digits):
    count = 0
    for k in digits:
        if contains_digit(number, k):
            count += 1
    if count == len(digits):
        return True
    else:
        return False
